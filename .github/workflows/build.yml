name: build-readme

on:
  push:
    branches: [main]
    paths:
      - "data/links.csv"
      - "scripts/build.py"
      - "docs/index.html"
      - ".github/workflows/build.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build (validates CSV, regenerates README + embeds site data)
        run: python scripts/build.py

      - name: Commit regenerated files if changed
        run: |
          if git diff --quiet -- README.md docs/index.html; then
            echo "No generated files changed; nothing to commit."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add README.md docs/index.html
            git commit -m "chore: regenerate README and embed site data from links.csv [skip ci]"
            git push
          fi
